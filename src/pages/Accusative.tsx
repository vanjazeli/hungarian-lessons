import { AccusativePanel } from "features";
import { MainLayout } from "components";

export const Accusative = () => {
  return (
    <MainLayout>
      <AccusativePanel />
    </MainLayout>
  );
};
